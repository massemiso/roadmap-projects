services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: personal-blog
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:sqlite:/app/data/blog.db
    volumes:
      - ./data:/app/data